---
name: reflect
disable-model-invocation: true
description: >
  retro（dotfiles スキル改善 issue 起票）と session-feedback（行動・記憶のふりかえり）を
  1 パスで統合起動するふりかえりスキル。1 タスク/セッションの終わりに `/reflect` で
  明示起動する。スキル指摘は retro と同じ経路で crgstar/dotfiles（PUBLIC）の issue へ、
  行動・記憶の改善はローカル（memory / CLAUDE.md 等）へ書き分ける。単体で使いたいときは
  既存の `/retro` `/session-feedback` が残っている。`--auto <transcript>` は launchd 経由の
  無人実行専用モード（§6。人間の確認を二重サニタイズ監査に置き換え、結果はマーカー付き
  stdout で返す）。
---

# タスクふりかえり — retro + session-feedback を 1 パスで統合起動

**main は生ログを読まない**（§2 で隔離サブエージェントに委譲）。送信先が PUBLIC であることの漏洩規律は retro 冒頭と同じ（参照）。

`--auto` 付き起動（無人モード）は §1〜§5 に §6 の差分を重ねて実行する。`--auto` の最終メッセージは 0 件・エラー時も例外なく §6 のマーカー形式だけで返す（読み手は人間でなくパーサ）。引数なしの対話版に §6 は適用しない。

## §1. 事前チェック

`retro` SKILL.md §1 をそのまま実行する（参照。写経しない）。

## §2. 抽出（retro-extractor を 1 回起動）

`retro-extractor` エージェントを 1 回起動する。渡すものは retro §2 の一式に加え、次の 3 つを追加する:

1. **このファイル（reflect SKILL.md）の絶対パス** — extractor に本節の B 仕様・A/B 振り分けルールを追加の作業仕様として読ませる
2. **機械抽出コマンドの実行指示**: `session-feedback-extract <§1 で特定した jsonl の絶対パス>` を実行させる（読み取り専用スクリプト。出力内容の正は session-feedback §1.1）。出力を B 系シグナルの一次材料として使わせる
3. **session-feedback SKILL.md の絶対パス** — B の Category 判定に §1.2 のカテゴリ表を読ませる

### 返却フォーマット

```text
## A: スキル指摘（サニタイズ済み・公開可）
### 指摘 1
（retro §2.2 のスキーマ）
...
指摘件数: <N>

## B: 行動・記憶（ローカル適用のみ・非公開）
### 行動 1
**分類/推奨先:** <§1.2 のカテゴリ> / <保存先の対応表の場所 1 つ（新規 or 更新）>
**内容:** <起きたこと + 根本原因を 1 文>
**書き込み案:** <書く本文そのもの。最小限。既存行の変更は before/after のみ>
...
行動件数: <M>

B は簡潔に書く。
```

### A/B の振り分けルール

dotfiles 管理スキルの SKILL.md の記述（または欠如）に帰属できるシグナルは **A のみ**（process-retro が直すので B に重複させない）。帰属できない行動・記憶・ドキュメント不整合・権限見直しは **B**。session-feedback の「使用スキル分析」のうち dotfiles スキルの本文改善は A に吸収され、新規スキル候補・トリガー漏れだけが B（案内のみ）に残る。

### B の採用バー

採用基準は session-feedback §1.2「抽出のコツ」に従う（参照）。reflect 固有の追加: 一回性（その場の文脈・好みに留まるもの）は落とす。推奨アクションは 1 件につき 1 つに絞る（複数案の併記はしない — 往復を消すのが目的）。

B は原則サニタイズ不要（ローカルにしか書かない）。**例外**: 書き込み先が dotfiles 管理ファイル（`~/.claude/CLAUDE.md` 等、PUBLIC リポに追跡されるもの）の B 項目は、書き込み案にも retro §3 のサニタイズを適用する。A に入るのは A ブロックだけという分離は絶対（issue 本文組み立てで B を混ぜない）。

エラー返却形は retro §2 と同じ（参照）。retro §2.3 の 0 件スキップは **A・B とも 0 件のときだけ**適用する（片方だけ 0 件でも §3 の確認は行う）。

## §3. 一括確認（1 往復）

extractor の返却を**一字一句そのまま**テキスト出力する（main は要約・整形しない。A はこのプレビューが唯一のサニタイズ検知機会（retro §4 と同じ）、B はユーザが書き込み原文を確認できる唯一の機会）。続けて 1 つのプロンプトで両方の判断を仰ぐ（`AskUserQuestion` は使わない）:

```text
送信先: crgstar/dotfiles（GitHub issue・PUBLIC）
選んだ jsonl: <§1 で特定したパス>
A: このまま送ってよいですか？（送る / 修正指示 / やめる）
B: 適用する番号を指定してください（例: 全部 / 1,3 / なし）
```

修正指示が来たら反映して**全ブロックを再掲**して仰ぎ直す（retro §4 と同じ）。ただし修正指示でも **B の内容を A に移さない**（main はサニタイズできない。A に載せたいシグナルは §2 の抽出からやり直す）。

## §4. 適用

**A**: retro §4「本文の組み立てと送信」に従う（参照）。issue 本文に入れるのは A ブロック内の `### 指摘 N` 群と `指摘件数:` 行だけ（`## A:` 見出しは外す。process-retro のパーサ互換のため）。

**B**: 承認された番号だけ書き込む。memory は auto-memory 規約（MEMORY.md index 更新含む）、CLAUDE.md / 既存ファイル編集は session-feedback §3.4 の注意に従う（参照）。

`~/.claude/CLAUDE.md` は dotfiles（PUBLIC）管理なので、追記内容に作業リポ固有情報を書かない（コミット時の sanitize 規約が最終防壁だが、書かないのが先）。

末尾サマリ（必ず 1 行）:

```text
ふりかえり: スキル指摘 <N> 件（送信済み|送信なし|失敗） / 行動 <M> 件中 <K> 件適用
```

## §5. エラー処理

A 系は retro §5 に準拠（参照）。B 系は項目単位で失敗を記録してスキップし、実行全体は止めない。このスキルのエラーで他の作業をブロックしない。

## §6. 無人モード（`--auto <transcript>`）

`/reflect --auto <セッション jsonl の絶対パス> [--since-line <N>]` は `run-headless.sh` からのヘッドレス起動専用。対話版との差分だけ示す（示していない箇所はすべて対話版と同じ）。

- **§1 の置換**: jsonl は引数の transcript を使う（`CLAUDE_CODE_SESSION_ID` は別セッションなので使わない）。retro §1.1 の `gh auth status` はスキップ（投稿はドライバの責務）。retro §1.4 の mirugit annotation はスキップ（終了済みセッションの token は特定できない）。
- **`--since-line <N>` 付きの再処理**: このセッションは行 N まで処理済み。extractor へ渡す指示に次を追加する: 「jsonl の行 1〜N は前回処理済み。文脈として読んでよいが、**行 N 以前のやりとりだけに根拠を持つシグナルは A・B とも拾わない**。行 N より後に根拠（訂正・反復・拒否等の発話）が現れたものだけを対象にする」。境界を跨いで同根のシグナルが再抽出された場合の保険は retro §1.5 の既存 open issue 重複チェック（従来通り実施）。
- **§3 の置換（人間プレビューなし）**: extractor の返却をテキスト出力せず、A ブロック全文を `sanitize-auditor` に **2 回、独立に**検査させる（`Agent` ツールで別々に起動し、一方の結果を他方に渡さない）。渡す入力は (1) 検査対象 = A ブロック全文（diff の代わり） (2) 判定基準 = `~/dotfiles/.claude/skills/process-retro/SKILL.md` の絶対パス。判定:
  - **両方 clean** → A は「投稿可」（REFLECT-OUTBOX で返す）
  - **どちらか一方でも漏洩あり・判断不能・返却不正** → A 全体を「保留」（REFLECT-HOLD で返す。部分投稿はしない）
- **§4 の置換（書き込み・投稿をしない）**: Write / `gh api` / `rm` を使わない（権限もない）。結果は下記マーカーの最終メッセージだけで返し、ファイル保存・issue 投稿・再送はドライバが行う。
- B のうち**推奨先が memory のもの**は `REFLECT-MEMORY` ブロック（後述）で返す。書き込みはドライバが行う。
- **それ以外の B**（CLAUDE.md・任意ドキュメント・権限見直し等、dotfiles 管理ファイルに限らない対象宛）は `REFLECT-PROPOSAL` ブロック（後述）で返す。保存はドライバが行う（人間の採用/破棄判断は常設ビューアが担う。本スキルはファイル移動や対象ファイルへの適用を行わない）。
  - **target は必ず実在しうる具体的な絶対パス 1 つに落とす**。権限見直しや運用方針のように特定ファイルに直結しない教訓は、その方針を書き込むべき統轄ドキュメント（例: 権限なら該当 `settings.json`、汎用の作法なら効かせたい階層の `CLAUDE.md`）を target に選ぶ。どのファイルにも紐づけられないものは提案として成立しないので**落とす**（空・相対 target で返すとドライバが毎晩 hold に積むだけになる。ビューアに乗る形にできないなら §2 の「迷ったら落とす」で処理する）。
- memory の書き込み先ディレクトリは「引数 transcript と同じディレクトリ直下の `memory/`」（= `dirname(transcript)/memory/`。プロジェクト名のエスケープ計算はしない）。
- **重複抑止**: B の各項目を確定する前に、(1) 上記 memory ディレクトリの `MEMORY.md` と関連既存ファイル、(2) `~/dotfiles/.local/reflect-proposals/pending/` の既存提案ファイルの title/target を読む。同趣旨が既にあれば新規作成せず、memory 宛は `mode: update`（既存ファイルの全文置換）にし、提案宛はその項目自体を落とす（提案には update の仕組みがない）。人間確認が無いので、memory・提案のどちらも**迷ったら落とす**（採用バーは §2 のまま。人間確認が消える分をここで補う）。
- ファイル本文は auto-memory 規約に従う（frontmatter: `name` / `description` / `metadata.type`、body に **Why:** / **How to apply:**）。
- **出力形式の全面上書き**: 対話版が規定する素のテキスト出力 — §2.3 の 0 件スキップ時の末尾サマリ、§4 の末尾サマリ、§5 / retro §5 のエラー報告 — は**すべて下記マーカー形式に置き換える**。0 件でも REFLECT-SUMMARY マーカーで返す（素の 1 行サマリを返すとドライバがパース不能として hold に落とし、0 件のたびに人間確認が発生してしまう）。

### 返却フォーマット

最終メッセージはマーカーブロックだけで構成する（マーカー外に文章・前置き・説明を一切置かない。各マーカーは単独行）。

投稿可の A があるとき（1 行目は `title:`、2 行目以降が issue 本文。本文の形は retro §4「本文の組み立てと送信」と同じ）:

```text
<<<REFLECT-OUTBOX
title: [ふりかえり] dotfiles スキル: <N> 件の指摘（<YYYY-MM-DD>）
# dotfiles スキル ふりかえり（自動生成）
<§2.2 の指摘ブロック群 + 指摘件数行>
REFLECT-OUTBOX>>>
```

保留のとき（A ブロック全文 + 監査 2 回の判定要旨。人間が朝に判断する材料）:

```text
<<<REFLECT-HOLD
<A ブロック全文>
<監査1・監査2 の判定要旨>
REFLECT-HOLD>>>
```

memory 宛の B があるとき（1 件 1 ブロック。複数件はブロックを繰り返す）:

```text
<<<REFLECT-MEMORY
mode: create
file: <memory ファイルの絶対パス>
index: - [<Title>](<ファイル名>.md) — <一行 hook>
---
<memory ファイル全文（frontmatter 含む）>
REFLECT-MEMORY>>>
```

- `mode` は `create` | `update`。`update` は全文置換。
- `create` では `index` 必須。`update` のとき index 行は既存 index があるので省略可（`index:` 行自体は残し値を空にする）。
- 本文にマーカー行と同形の行（行頭 `<<<REFLECT-` / 行頭 `REFLECT-…>>>`）をそのまま書かない。引用が必要なときは行頭に空白を 1 つ置く。

提案宛の B があるとき（1 件 1 ブロック。複数件はブロックを繰り返す）:

```text
<<<REFLECT-PROPOSAL
target: <変更対象の絶対パス>
kind: <claude-md | skill | doc | permission | other>
title: <一行要約>
---
## 理由

<なぜこの変更が要るか。発生した事象 + 根本原因を簡潔に>

## 変更例

<そのまま適用できる具体形>
REFLECT-PROPOSAL>>>
```

- frontmatter（`id` / `status` / `source_session` / `created` 等）は**ドライバが付与する。ここには書かない**（形式ゆれ防止）。
- 「## 変更例」は既存行の変更を ` ```before ` + ` ```after ` のフェンスペア、追記は ` ```append ` フェンスで示す（提案ビューアがこの種別で diff/追記表示を出し分ける）。フェンスは CommonMark の長さルールに従う（閉じは開始と同数以上のバッククォート）。本文自体に ` ``` ` を含む場合は 4 連バッククォート ` ````before ` で囲む。それ以外（新規ファイルの全文提案等）は素の内容をそのまま書く。
- **1 提案 1 変更**: 変更例は `before`/`after` ペアを高々 1 組、または `append` 1 つに絞る。ビューアは先頭の 1 変更しか描画・審査できないため、複数の別々な編集は**別々の REFLECT-PROPOSAL ブロックに分ける**（それぞれ独立に採用/破棄できる）。1 ブロックに複数ペアを詰めると、ユーザが見ていない編集が採用時に適用されてしまう。
- 本文にマーカー行と同形の行（行頭 `<<<REFLECT-` / 行頭 `REFLECT-…>>>`）をそのまま書かない。引用が必要なときは行頭に空白を 1 つ置く（REFLECT-MEMORY と同じ注意）。

常に最後に（A・B とも 0 件でもこれだけは返す）:

```text
<<<REFLECT-SUMMARY
ふりかえり: スキル指摘 <N> 件（投稿可 | 保留 | 0 件） / 行動 <M> 件（memory 書き込み <K> 件 / 提案 <P> 件）
REFLECT-SUMMARY>>>
```

エラー時（§5 相当）も `ふりかえり: エラー — <一行の説明>` を REFLECT-SUMMARY で返す。SUMMARY を欠いた出力はドライバがパース不能として保留に落とす。

## §6a. 再提案（regenerate）モード

`run-headless.sh` の再提案サイクル（設計書 `reflect-b-autoconsume-design.md` 決定13・13a）専用の呼び出し形。夜間 run の queue 処理の前、または手動即時実行 `--regenerate-only` から、pending の `status: regenerate` 1 件ごとに 1 回このモードで headless claude を起動する。§6 との差分だけ示す（示していない箇所は §6 と同じ。マーカー形式・フェンス規約・「1 提案 1 変更」等はすべて踏襲する）。

- **入力**: transcript ファイルではなく、ドライバがプロンプト本文に直接埋め込む。埋め込まれる文脈は次の 2 つ:
  1. **元提案ファイルの全文**（frontmatter を含む。`target` / `kind` / `title` / `note` / `## 理由` を含む）
  2. **target の現在の内容**（ドライバが読めた場合は全文。読めない・存在しない場合は「target 不在」である旨を明示したプレースホルダに置き換わる）
- **やること**: 上記 2 つを踏まえ、元の変更例がなぜ古くなったか（`note` の指摘・target 側の変化・hook エラー等）を汲み、target の現在の内容に対して素直に適用できる変更例を作り直す。`target` / `kind` および `title` が指す大まかな意図は維持してよいが、`## 変更例` は作り直してよい（前提が変わっているので、旧 before/after にこだわらない）
- **出力**: §6 の `REFLECT-PROPOSAL` ブロックをちょうど 1 個だけ返す。マーカー外に文章を一切置かない。`REFLECT-SUMMARY` 等の他マーカーは不要（このモードは 1 提案の作り直し専用で、A 系・memory 系の判断は行わない）。`id` / `supersedes` 等の frontmatter はここでも書かない（ドライバが付与する。§6 の frontmatter 規約と同じ）
- **作り直しに失敗する**場合（target 不在で変更例が成立しない・判断がつかない等）は、無理に空や不完全な提案を返さず `REFLECT-PROPOSAL` ブロック自体を省略してよい。ドライバは「ブロック欠落 = 失敗」として元提案を `status: regenerate` のまま pending に残す（決定13a: ロールバックすべき副作用が無いため安全。次回の再提案サイクルでまた拾われる）
