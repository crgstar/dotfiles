---
name: skill-md-guide
description: >
  SKILL.md の作成・改修ガイド。新規スキル作成・既存スキルの簡潔化・description
  改善時に自動適用する。「SKILL.md を作って」「スキルを簡潔に」「description を
  改善」「スキルを圧縮」等で使用。skill-creator と併用 (フロー全体は skill-creator、
  書き味の原則は本スキル)。ただし通常の markdown 編集・スキル選択の相談だけのときは対象外。
---

## 基本原則

各行が「初見の Claude Code の判断・行動を変えるか？」を満たすよう SKILL.md を書く。
長く正確に書くことよりも、短く抽象的ではあるが印象に残る書き方をする。

ルール・表・チェックリストを書きたくなったら一旦止めて、スタンス 1 文にできないか試す。
迷ったら短い方を選ぶ。

ただし「印象に残る」 を警句や煽り文句で達成しない。抽象ルールには why を 1 文添える。

## description は 3 要素

対象物 + 発動条件 + 除外条件 を揃える。動詞中心ではなく対象物で語ると誤発動が減る。

## 書いたら subagent でレビュー

**適用規模**: 新規スキル作成・3 行以上の変更・手順ロジックの変更は必須。1〜2 行の文言微修正は省略可（省略した場合はその旨を明示する）。

general-purpose の subagent を起動し、その内部で `skill-creator:skill-creator` スキルを invoke して本ガイドの観点でレビューを依頼する（スキル名は `subagent_type` 引数ではなく、subagent 内で Skill ツールを使って呼ぶ）。
指摘を反映 → 再レビューを最大 3 回。新規指摘が 0 件かつ前周と同じ指摘だけ残るなら収束。3 周で収まらなければ残件をリストでユーザに渡す。
